---
// AMMW Content
import { getAssetUrl } from "../utils/cdn";
---

<section
  class="min-h-screen w-full relative flex flex-col justify-center items-center text-white py-8 px-6 md:py-12 md:px-12 uppercase"
>
  <div
    class="h-dvh flex flex-col items-center justify-center relative z-20 gap-10"
  >
    <div class="flex items-center justify-center relative px-16">
      <img
        id="tubelight"
        src={getAssetUrl("/pink_tubelight.webp")}
        alt="violet light"
        class="acmw-tubelight w-[80vw] max-w-[1400px] opacity-0 select-none absolute z-[-1]
                    [mask-image:radial-gradient(circle,black_30%,transparent_100%)]
                    [mask-repeat:no-repeat]
                    [mask-position:center]
                    [mask-size:contain]
                    will-change-opacity
                    max-[480px]:w-[95vw] max-[480px]:max-w-none max-[480px]:left-1/2 max-[480px]:-translate-x-1/2"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div class="flex items-center gap-10">
      <img
        src={getAssetUrl("/acmw-logo.webp")}
        alt="ACM-W Logo"
        class="acmw-logo w-[30vw] h-auto max-[480px]:!w-[45vw] max-[480px]:max-w-none"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div
      class="text-left text-white text-base leading-relaxed max-w-4xl font-polysans
                max-[640px]:text-[0.95rem] max-[640px]:leading-[1.45] max-[640px]:max-w-[28rem]
                max-[480px]:text-[0.85rem] max-[480px]:leading-[1.4]"
    >
      <span class="text-primary-pink"
        >ACM-W was created with a simple yet profound mission: to create equal
        access to opportunities in technology and ensure that everyone,
        regardless of gender, has the chance to grow in STEM.<br />
        We believe talent and potential aren&apos;t defined by barriers, but by the
        right support, mentorship, and exposure.<br /></span
      ><br />
      As a sister chapter of ACM-VIT, we are a part of a community that values contributions
      across all computing fields. Through our InspiHER podcast series, blogs, and
      community stalls, we&apos;ve carved out a space where stories and perspectives
      are shared and voices in tech are amplified.<br />
      We stand for inclusion, empowerment and growth, encouraging women to take initiative,
      share their ideas, and thrive in computing fields, shoulder to shoulder with
      their peers.
    </div>

    <!-- Learn More Button -->
    <a
      href="https://women.acm.org"
      target="_blank"
      rel="noopener noreferrer"
      class="border border-primary-pink text-white px-8 py-3 rounded-full text-sm hover:cursor-pointer hover:scale-105 transition-transform duration-300 ease-in-out font-polysans
                max-[640px]:text-[0.85rem]
                max-[480px]:py-[0.55rem] max-[480px]:px-4 max-[480px]:text-[0.8rem]
                inline-block text-center"
    >
      LEARN MORE
    </a>
  </div>
</section>

<style>
  .font-polysans {
    font-family: "PolySans Trial";
  }

  .acmw-tubelight {
    filter: hue-rotate(285deg) saturate(0.72) brightness(0.72);
  }

  .acmw-logo {
    filter: hue-rotate(285deg) saturate(0.72) brightness(0.94);
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("DOMContentLoaded", () => {
    const tubelight = document.getElementById("tubelight");

    if (tubelight) {
      const tl = gsap.timeline({
        scrollTrigger: {
          trigger: tubelight,
          start: "top 80%",
          end: "top 50%",
          scrub: false,
          once: true,
        },
      });

      // Flickering effect
      tl.to(tubelight, {
        opacity: 0.3,
        duration: 0.05,
        ease: "none",
      })
        .to(tubelight, {
          opacity: 0,
          duration: 0.05,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.5,
          duration: 0.08,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.1,
          duration: 0.05,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.6,
          duration: 0.1,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.2,
          duration: 0.06,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.7,
          duration: 0.12,
          ease: "none",
        })
        .to(tubelight, {
          opacity: 0.3,
          duration: 0.05,
          ease: "none",
        })
        //final glow
        .to(tubelight, {
          opacity: 0.8,
          duration: 0.3,
          ease: "power2.out",
        })
        .to(tubelight, {
          opacity: 0.75,
          duration: 0.15,
          ease: "sine.inOut",
          yoyo: true,
          repeat: 1,
        });
    }
  });
</script>
