---
import PartnerLaptopSingular from "../components/PartnerLaptopSingular.astro";
import { reader } from "../utils/reader";
import { getAssetUrl } from "../utils/cdn";

const partnersData = await reader.singletons.partners.read();
const partners = partnersData?.items ?? [];

const marqueeRepeat = 4;
const marqueeRows = [
  {
    items: partners.filter((_, index) => index % 2 === 0),
    reverse: false,
  },
  {
    items: partners.filter((_, index) => index % 2 === 1),
    reverse: true,
  },
];
---

<div
  id="partners-container"
  class="relative flex flex-wrap gap-6 justify-center items-center w-full pt-18 pb-64 overflow-hidden"
>
  <div
    id="Cassette-pos-1"
    class="partners-floating w-full flex justify-center mt-0 relative"
  >
    <img
      id="partners-svg"
      src={getAssetUrl("/partners-cassette.webp")}
      alt="Partners Image"
      class="partners-title-cassette max-w-[300px] md:max-w-[500px] w-auto object-contain"
      loading="lazy"
      decoding="async"
    />
  </div>

  <div
    class="partners-carousel w-[80vw] pb-24 flex flex-wrap gap-6 justify-center items-center relative z-10"
  >
    <div class="partners-track flex flex-wrap gap-6 justify-center items-center">
      {
        partners.map((partner, index) => (
          <div class="partners-card">
            <PartnerLaptopSingular title={partner.title} index={index} />
          </div>
        ))
      }
    </div>
    <div class="partners-marquee">
      {marqueeRows.map((row) => (
        <div
          class={`partners-marquee-row${row.reverse ? " is-reverse" : ""}`}
        >
          {Array.from({ length: marqueeRepeat }).map((_, index) => (
            <div
              class="partners-marquee-group"
              aria-hidden={index === 0 ? undefined : "true"}
            >
              {row.items.map((partner, partnerIndex) => (
                <div class="partners-card">
                  <PartnerLaptopSingular
                    title={partner.title}
                    index={partnerIndex}
                  />
                </div>
              ))}
            </div>
          ))}
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  .partners-floating {
    position: fixed;
    left: 0;
    right: 0;
    bottom: -140px;
    z-index: 20;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    will-change: opacity;
  }

  .partners-title-cassette {
    filter: hue-rotate(285deg) saturate(0.72) brightness(0.94);
  }

  .partners-floating.is-fixed {
    opacity: 1;
  }

  .partners-marquee {
    display: none;
  }

  .partners-marquee-row {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: var(--gap);
  }

  .partners-marquee-row.is-reverse .partners-marquee-group {
    animation-direction: reverse;
  }

  .partners-marquee-group {
    display: flex;
    flex-shrink: 0;
    align-items: center;
    flex-wrap: nowrap;
    gap: var(--gap);
    animation: partners-marquee var(--duration) linear infinite;
    will-change: transform;
  }

  @keyframes partners-marquee {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(calc(-100% - var(--gap)));
    }
  }

  @media (max-width: 768px) {
    #partners-container {
      overflow: visible;
    }

    .partners-floating {
      bottom: -110px;
    }
    .partners-carousel {
      width: 100vw;
      padding: 0 6vw 5rem;
      overflow: visible;
    }

    .partners-card {
      flex: 0 0 auto;
    }

    .partners-track {
      display: none;
    }

    .partners-marquee {
      display: flex;
      flex-direction: column;
      gap: var(--gap);
      padding: 0.5rem 1vw;
      --duration: 40s;
      --gap: 1.25rem;
    }
  }
</style>

<script>
  const initializePartners = () => {
    const container = document.getElementById("partners-container");
    const pos1 = document.getElementById("Cassette-pos-1");
    const section = document.getElementById("partners");
    const gallery = document.getElementById("gallery");

    if (container && container.dataset.laptopRevealReady !== "true") {
      container.dataset.laptopRevealReady = "true";
      const laptops = Array.from(
        container.querySelectorAll<HTMLElement>("[data-partner-laptop]")
      );
      const revealLaptops = () => {
        laptops.forEach((laptop) => laptop.classList.add("is-open"));
      };

      if ("IntersectionObserver" in window) {
        const laptopObserver = new IntersectionObserver(
          ([entry], observer) => {
            if (!entry.isIntersecting) return;
            revealLaptops();
            observer.disconnect();
          },
          { rootMargin: "-12% 0px -12% 0px", threshold: 0.18 }
        );
        laptopObserver.observe(container);
      } else {
        revealLaptops();
      }
    }

    if (!pos1 || !section || pos1.dataset.visibilityReady === "true") return;
    pos1.dataset.visibilityReady = "true";

    let inPartners = false;
    let inGallery = false;
    const updateVisibility = () => {
      const isActive = inPartners && !inGallery;
      pos1.classList.toggle("is-fixed", isActive);
      document.body.classList.toggle("partners-active", isActive);
    };

    const partnersObserver = new IntersectionObserver(
      ([entry]) => {
        inPartners = entry.isIntersecting;
        updateVisibility();
      },
      {
        root: null,
        rootMargin: "-15% 0px -15% 0px",
        threshold: 0,
      }
    );

    partnersObserver.observe(section);

    if (gallery) {
      const galleryObserver = new IntersectionObserver(
        ([entry]) => {
          inGallery = entry.isIntersecting;
          updateVisibility();
        },
        {
          root: null,
          rootMargin: "0px 0px 0px 0px",
          threshold: 0,
        }
      );

      galleryObserver.observe(gallery);
    }
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initializePartners, {
      once: true,
    });
  } else {
    initializePartners();
  }

  window.addEventListener("app:variants-mounted", initializePartners);
</script>
